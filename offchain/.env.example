# Google AI Studio API Key
# Get your key at: https://aistudio.google.com/app/apikey
GOOGLE_AI_STUDIO_API_KEY=your_api_key_here

# Agent Wallet Private Key
# This wallet submits transactions to the ComputeRouter contract
# and processes payments from users (User -> Agent -> Escrow)
# Must be funded with ADI testnet tokens for gas
# The agent address is derived from this private key
AGENT_PRIVATE_KEY=0x...

# Optional: Agent Model Configuration
AGENT_MODEL=gemini-2.0-flash
AGENT_NAME=NectoRoutingAgent

# Optional: Contract Address (already set in code, but can override)
# COMPUTE_ROUTER_ADDRESS=0x...
# ESCROW_CONTRACT_ADDRESS=0x...
# TESTNET_USDC_CONTRACT=0x...

# Optional: ADI Testnet RPC URL
# ADI_TESTNET_RPC_URL=https://rpc.ab.testnet.adifoundation.ai/
